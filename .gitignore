node_modules/
.pnpm-store/

# Build output
.next/
out/
dist/
.turbo/
*.tsbuildinfo

# Env — never commit secrets (CLAUDE.md rule 5). Only .env.example is tracked.
.env
.env.*
!.env.example

# Test artefacts
coverage/
playwright-report/
test-results/
blob-report/
.playwright/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Local scratch
*.log

# CDK synth output
infra/cdk.out
e2e/.artifacts/
apps/web/.local-renders/

# Claude Code personal settings
.claude/settings.local.json

.vercel
.impeccable/

# Subagent-driven development scratch (ledgers, briefs, review packages)
.superpowers/

# Live set harness output (decision 275, Task 13): images, prompts and costs
# from a real run against Gemini, never committed.
live-set-runs/

# Claude Code discovers the vendored agent skills through symlinks the
# `skills` CLI creates; this git cannot store symlinks, so re-run
# `npx skills add remotion-dev/skills` after a fresh clone.
.claude/skills/
