node_modules
.next
dist
.turbo
coverage
playwright-report
test-results
pnpm-lock.yaml
packages/db/drizzle

# Generated by Next and marked "should not be edited". Formatting it makes
# every dev-server start show a dirty working tree.
apps/web/next-env.d.ts

# Authored specification documents — formatted by hand, not by tooling.
docs

# Golden test artefacts: their bytes are owned by the compiler under test
# (REGEN_GOLDEN=1 regenerates them), never by the formatter. Prettier
# rewriting one after the last test run is exactly how CI went red on
# 6b54724 while local was green.
packages/timeline/src/golden/
packages/compositions/src/snapshot/golden/

# Bundled world geometry (Natural Earth 110m, public domain): one compact
# line by design — prettier expanding it to thousands of lines would bloat
# every diff that touches the file.
packages/compositions/src/geo/world-land.json
